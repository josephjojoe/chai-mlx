=== confidence_head.single_to_pair_proj ===
def forward(self,
    token_single_input_repr: Tensor) -> Tensor:
  weight = self.weight
  _0 = torch.linear(token_single_input_repr, torch.to(weight, 15))
  return _0


=== confidence_head.atom_distance_bins_projection ===
def forward(self,
    input: Tensor) -> Tensor:
  weight = self.weight
  _0 = torch.to(weight, 15)
  return torch.linear(torch.to(input, 15), _0)


=== confidence_head.blocks.0.attention_pair_bias.single_layer_norm ===
def forward(self,
    token_single_repr: Tensor) -> Tensor:
  bias = self.bias
  weight = self.weight
  _0 = torch.layer_norm(torch.to(token_single_repr, 6), [384], weight, bias)
  return _0


=== confidence_head.blocks.0.attention_pair_bias.attention.input2qkvg ===
def forward(self,
    argument_1: Tensor) -> Tensor:
  weight = self.weight
  _0 = [torch.to(argument_1, 15), torch.to(weight, 15)]
  return torch.einsum("fae,ebcd->bfcad", _0)


=== confidence_head.blocks.0.attention_pair_bias.attention.output_proj ===
def forward(self,
    argument_1: Tensor) -> Tensor:
  weight = self.weight
  out = torch.einsum("cdae,deb->cab", [argument_1, torch.to(weight, 15)])
  return out


=== confidence_head.blocks.1.attention_pair_bias.single_layer_norm ===
def forward(self,
    token_single_repr: Tensor) -> Tensor:
  bias = self.bias
  weight = self.weight
  _0 = torch.layer_norm(torch.to(token_single_repr, 6), [384], weight, bias)
  return _0


=== confidence_head.blocks.1.attention_pair_bias.attention.input2qkvg ===
def forward(self,
    argument_1: Tensor) -> Tensor:
  weight = self.weight
  _0 = [torch.to(argument_1, 15), torch.to(weight, 15)]
  return torch.einsum("fae,ebcd->bfcad", _0)


=== confidence_head.blocks.1.attention_pair_bias.attention.output_proj ===
def forward(self,
    argument_1: Tensor) -> Tensor:
  weight = self.weight
  out = torch.einsum("cdae,deb->cab", [argument_1, torch.to(weight, 15)])
  return out


=== confidence_head.blocks.2.attention_pair_bias.single_layer_norm ===
def forward(self,
    token_single_repr: Tensor) -> Tensor:
  bias = self.bias
  weight = self.weight
  _0 = torch.layer_norm(torch.to(token_single_repr, 6), [384], weight, bias)
  return _0


=== confidence_head.blocks.2.attention_pair_bias.attention.input2qkvg ===
def forward(self,
    argument_1: Tensor) -> Tensor:
  weight = self.weight
  _0 = [torch.to(argument_1, 15), torch.to(weight, 15)]
  return torch.einsum("fae,ebcd->bfcad", _0)


=== confidence_head.blocks.2.attention_pair_bias.attention.output_proj ===
def forward(self,
    argument_1: Tensor) -> Tensor:
  weight = self.weight
  out = torch.einsum("cdae,deb->cab", [argument_1, torch.to(weight, 15)])
  return out


=== confidence_head.blocks.3.attention_pair_bias.single_layer_norm ===
def forward(self,
    token_single_repr: Tensor) -> Tensor:
  bias = self.bias
  weight = self.weight
  _0 = torch.layer_norm(torch.to(token_single_repr, 6), [384], weight, bias)
  return _0


=== confidence_head.blocks.3.attention_pair_bias.attention.input2qkvg ===
def forward(self,
    argument_1: Tensor) -> Tensor:
  weight = self.weight
  _0 = [torch.to(argument_1, 15), torch.to(weight, 15)]
  return torch.einsum("fae,ebcd->bfcad", _0)


=== confidence_head.blocks.3.attention_pair_bias.attention.output_proj ===
def forward(self,
    argument_1: Tensor) -> Tensor:
  weight = self.weight
  out = torch.einsum("cdae,deb->cab", [argument_1, torch.to(weight, 15)])
  return out


=== confidence_head.plddt_projection ===
def forward(self,
    input: Tensor) -> Tensor:
  weight = self.weight
  _0 = torch.to(weight, 15)
  return torch.linear(torch.to(input, 15), _0)


=== confidence_head.pae_projection ===
def forward(self,
    input: Tensor) -> Tensor:
  weight = self.weight
  _0 = torch.to(weight, 15)
  return torch.linear(torch.to(input, 15), _0)


=== confidence_head.pde_projection ===
def forward(self,
    input: Tensor) -> Tensor:
  weight = self.weight
  _0 = torch.to(weight, 15)
  return torch.linear(torch.to(input, 15), _0)

